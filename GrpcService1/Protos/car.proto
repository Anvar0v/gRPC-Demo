syntax = "proto3";

option csharp_namespace = "GrpcService1.Protos";

service Car{
	rpc CarInfo(CarRequest) returns (CarResponse);
}

message CarRequest{
	string a = 1;
}

message CarResponse{
	string a = 1;
}